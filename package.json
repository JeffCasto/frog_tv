{
  "name": "frogtv-live",
  "version": "1.0.0",
  "description": "A surreal interactive livestreaming platform with animated frogs",
  "private": true,
  "scripts": {
    "dev": "cd web && npm run dev",
    "build": "cd web && npm run build",
    "start": "cd web && npm start",
    "deploy:functions": "cd functions && npm run build && firebase deploy --only functions",
    "deploy:database": "firebase deploy --only database",
    "deploy:all": "npm run deploy:functions && npm run deploy:database",
    "functions:logs": "firebase functions:log",
    "install:all": "cd web && npm install && cd ../functions && npm install"
  },
  "keywords": [
    "frogs",
    "livestream",
    "interactive",
    "twitch",
    "firebase",
    "nextjs"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  }
}
